export type Repository = {
  id: number;
  name: string;
  full_name?: string;
  description?: string;
  homepage?: string;
  html_url?: string;
  language?: string;
  created_at: Date;
};

export type Myself = {
  //profile data
}
